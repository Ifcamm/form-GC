import { RowStyleDirective } from './row-style.directive';

describe('RowStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new RowStyleDirective();
    expect(directive).toBeTruthy();
  });
});
